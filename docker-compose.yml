version: '3.8'

services:
  webinfo:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
      - SECRET_KEY=change-this-in-production
      - API_RATE_LIMIT=60
      - CACHE_TTL=300
    restart: unless-stopped
